import { CUSTOM_COLORS } from '$lib/constants'
import { ColorItem, ColorPalette, Meta } from '@storybook/blocks'

<Meta title="Colors/Color Palette" />

<ColorPalette>
    {Object.entries(CUSTOM_COLORS).map(([color, values]) => {
        return <ColorItem key={color} title={color} colors={values} />
    })}
</ColorPalette>
